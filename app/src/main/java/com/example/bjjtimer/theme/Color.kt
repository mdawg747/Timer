package com.example.bjjtimer.theme

import androidx.compose.ui.graphics.Color

// ── Deep, premium dark palette ──────────────────────────────────────────────
val DarkBackground = Color(0xFF060A10)       // Near-black with blue undertone
val DarkSurface = Color(0xFF0F1520)          // Elevated surface
val DarkSurfaceVariant = Color(0xFF1A2233)   // Cards, chips
val DarkCardBackground = Color(0xFF141D2B)   // Slightly lifted cards

// ── Accent: warm championship gold with depth ──────────────────────────────
val BjjGold = Color(0xFFFFB800)              // Vibrant, saturated gold
val BjjGoldDark = Color(0xFFCC9400)          // Pressed / muted state
val BjjGoldLight = Color(0xFFFFD54F)         // Glow / highlight

// ── Timer state: tuned for OLED punch ──────────────────────────────────────
val TimerGreen = Color(0xFF00E676)           // Active round - electric green
val TimerGreenGlow = Color(0xFF69F0AE)       // Glow
val TimerYellow = Color(0xFFFFEA00)          // 1 minute left
val TimerYellowGlow = Color(0xFFFFF176)
val TimerRed = Color(0xFFFF1744)             // Last 10s - urgent red
val TimerRedGlow = Color(0xFFFF5252)
val TimerOrange = Color(0xFFFF9100)          // 30s warning

// ── Rest phase: cool, calming cyan-blue ────────────────────────────────────
val RestBlue = Color(0xFF00B0FF)
val RestBlueDark = Color(0xFF0091EA)
val RestBlueGlow = Color(0xFF40C4FF)

// ── Text hierarchy ─────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFF5F5F7)          // Apple-white
val TextSecondary = Color(0xFF8E95A2)
val TextTertiary = Color(0xFF5A6070)

// ── Belt colors for presets ────────────────────────────────────────────────
val BeltWhite = Color(0xFFF0F0F0)
val BeltBlue = Color(0xFF2979FF)
val BeltPurple = Color(0xFFAA00FF)
val BeltBrown = Color(0xFF8D6E4C)
val BeltBlack = Color(0xFF2A2A2A)

// ── Borders & dividers ─────────────────────────────────────────────────────
val BorderSubtle = Color(0xFF243040)
val BorderMuted = Color(0xFF1A2233)

// ── Gradient helpers ───────────────────────────────────────────────────────
val GradientStart = Color(0xFF0A1628)
val GradientEnd = Color(0xFF060A10)
val GlassWhite = Color(0x18FFFFFF)           // Frosted glass overlay
val GlassWhiteBorder = Color(0x28FFFFFF)     // Glass border highlight
